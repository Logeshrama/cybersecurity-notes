numbers = (1, 2, 3, 3) #in () we can store multiple values in a single variable. Tuples are immutable, meaning we cannot change their values after creation.
numbers.count(3) #count how many times 3 is present in the tuple
numbers.index(2) #find the index of 2 in the tuple